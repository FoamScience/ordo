# Markdown sample

Some intro text.

```python
print("compliant: language given")
```

```
violation: no language on this fence
```

<div>violation: raw inline HTML block</div>

More prose, no HTML here — compliant.
