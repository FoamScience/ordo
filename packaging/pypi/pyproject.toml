[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ordo"
version = "0.2.0"
description = "Comprehension-optimized ordering of code-change hunks — thin wrapper over the ordo-engine binary"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/FoamScience/ordo"

[project.scripts]
ordo-engine = "ordo.__main__:main"

[tool.hatch.build.targets.wheel]
packages = ["ordo"]
